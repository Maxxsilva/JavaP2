CREATE TABLE t_produto (ID BIGINT NOT NULL, DESCRICAO VARCHAR(255), INFRACAO INTEGER, PRECO FLOAT, PRIMARY KEY (ID))
CREATE TABLE t_dependente (ID BIGINT NOT NULL, DATANASC DATE, NOME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE t_funcionario (ID BIGINT NOT NULL, DATANASCIMENTO DATE, EMAIL VARCHAR(255), NOME VARCHAR(255), c_dependente BIGINT, PRIMARY KEY (ID))
ALTER TABLE t_funcionario ADD CONSTRAINT tfncnariocdpndente FOREIGN KEY (c_dependente) REFERENCES t_dependente (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
